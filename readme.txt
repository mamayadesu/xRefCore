Before to start:
1. Configure app.json
2. Create here directory "Program" and there class "Main"
    Then this folder must contains:
    Core/
    Program/
    app.json
    autoload.php
    readme.txt (you can delete it)